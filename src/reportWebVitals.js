// src/reportWebVitals.js
import { reportWebVitals } from 'web-vitals';

// You can keep the function to log the metrics if you want
const logVitals = (metric) => {
  console.log(metric);
};

export default logVitals;
